Self-Extracting Archive Packager
================================

Produces an archive that can be installed like so::

    chmod a+x myarchive.run
    sudo ./myarchive.run

The contents will my installed to ``/opt/myarchive``.

Configuration
-------------

No configuration options available.
